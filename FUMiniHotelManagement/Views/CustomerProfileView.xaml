<UserControl x:Class="FUMiniHotelManagement.Views.CustomerProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helper="clr-namespace:FUMiniHotelManagement.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryOrangeBrush" Color="#FF8C00"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#FFA500"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="DarkGrayBrush" Color="#333333"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="White"/>

        <Style x:Key="ProfileTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryOrangeBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InputLabelStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightGrayBrush}"/>
            <Setter Property="Background" Value="{StaticResource LightGrayBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryOrangeBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentOrangeBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryOrangeBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource PrimaryOrangeBrush}" Padding="20"
            HorizontalAlignment="Center" VerticalAlignment="Center">

        <Border Background="{StaticResource WhiteBrush}" Padding="30" 
                Width="450" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="CẬP NHẬT HỒ SƠ CÁ NHÂN" Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource ProfileTitleStyle}"/>

                <TextBlock Text="Tên Đầy Đủ:" Grid.Row="1" Grid.Column="0" Style="{StaticResource InputLabelStyle}"/>
                <TextBox Text="{Binding EditingCustomer.CustomerFullName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>

                <TextBlock Text="Điện Thoại:" Grid.Row="2" Grid.Column="0" Style="{StaticResource InputLabelStyle}"/>
                <TextBox Text="{Binding EditingCustomer.Telephone, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1"/>

                <TextBlock Text="Ngày Sinh:" Grid.Row="3" Grid.Column="0" Style="{StaticResource InputLabelStyle}"/>

                <DatePicker SelectedDate="{Binding EditingCustomer.CustomerBirthday, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1" Margin="0,5" Padding="8" BorderThickness="1" BorderBrush="{StaticResource LightGrayBrush}" Background="{StaticResource LightGrayBrush}" Foreground="{StaticResource DarkGrayBrush}"/>

                <TextBlock Text="Email:" Grid.Row="4" Grid.Column="0" Style="{StaticResource InputLabelStyle}"/>
                <TextBox Text="{Binding EditingCustomer.EmailAddress}" IsReadOnly="True" Grid.Row="4" Grid.Column="1" Foreground="Gray" Background="{StaticResource LightGrayBrush}"/>

                

                <Button Content="Lưu Hồ Sơ" Grid.Row="6" Grid.ColumnSpan="2" Margin="0,30,0,0" 
                        Command="{Binding SaveCommand}" Width="200" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </Border>
</UserControl>