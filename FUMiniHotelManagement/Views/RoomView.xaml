<UserControl x:Class="FUMiniHotelManagement.Views.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🔹 Header -->
        <TextBlock Text="Room Management"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0 0 0 15"/>

        <!-- 🔹 DataGrid hiển thị danh sách phòng -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Rooms}"
                  SelectedItem="{Binding SelectedRoom, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0 10 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomId}" Width="50"/>
                <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="120"/>
                <DataGridTextColumn Header="Description" Binding="{Binding RoomDetailDescription}" Width="200"/>
                <DataGridTextColumn Header="Max Capacity" Binding="{Binding RoomMaxCapacity}" Width="100"/>
                <DataGridTextColumn Header="Type ID" Binding="{Binding RoomTypeId}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding RoomStatus}" Width="80"/>
                <DataGridTextColumn Header="Price/Day" Binding="{Binding RoomPricePerDay}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 🔹 Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
            <Button Content="Reload" Width="100" Command="{Binding RefreshCommand}" Margin="0,0,10,0"/>
            <Button Content="Save" Width="100" Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
            <Button Content="Delete" Width="100" Command="{Binding DeleteCommand}" />
        </StackPanel>


    </Grid>
</UserControl>
