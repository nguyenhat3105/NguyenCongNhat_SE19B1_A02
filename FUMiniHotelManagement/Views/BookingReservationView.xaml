<UserControl x:Class="FUMiniHotelManagement.Views.BookingReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FUMiniHotelManagement.ViewModel"
             xmlns:local="clr-namespace:FUMiniHotelManagement.Views"
             MinWidth="800" MinHeight="500"
             FontFamily="Segoe UI" FontSize="13">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF6600"/>
        <SolidColorBrush x:Key="PrimaryBrushLight" Color="#FFF0E0"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
        <SolidColorBrush x:Key="HeaderTextBrush" Color="#666666"/>
        
        <DropShadowEffect x:Key="CardShadow" ShadowDepth="1" Opacity="0.1" BlurRadius="8" Color="#000000"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding Path=CornerRadius, RelativeSource={RelativeSource TemplatedParent}}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E65C00"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CC5200"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
    
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding Path=CornerRadius, RelativeSource={RelativeSource TemplatedParent}}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9F9F9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF8F2"/>
            
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        </Style>

        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="40"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrushLight}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style x:Key="CustomDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource DataGridHeaderStyle}"/>
            <Setter Property="RowStyle" Value="{StaticResource DataGridRowStyle}"/>
            <Setter Property="CellStyle" Value="{StaticResource DataGridCellStyle}"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="10" Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Button Content="Tạo đặt phòng" Command="{Binding ShowCreateDialogCommand}" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Hủy đặt phòng"
                    Command="{Binding CancelReservationCommand}"
                    CommandParameter="{Binding SelectedReservation.BookingReservationId}"
                    Style="{StaticResource SecondaryButton}"
                    Margin="8,0,0,0"/>
            <Button Content="Làm mới" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>

            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Từ:" Style="{StaticResource LabelTextBlock}" Margin="8,0,6,0"/>
                <DatePicker SelectedDate="{Binding FilterStart, Mode=TwoWay}" Width="140" VerticalAlignment="Center" Height="36"/>
                <TextBlock Text="Đến:" Style="{StaticResource LabelTextBlock}" Margin="10,0,6,0"/>
                <DatePicker SelectedDate="{Binding FilterEnd, Mode=TwoWay}" Width="140" VerticalAlignment="Center" Height="36"/>
                <Button Content="Áp dụng" Command="{Binding ApplyDateFilterCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Padding="10" Style="{StaticResource CardBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding Reservations}"
                              SelectedItem="{Binding SelectedReservation, Mode=TwoWay}"
                              Style="{StaticResource CustomDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding BookingReservationId}" Width="70"/>
                            <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"/>
                            <DataGridTextColumn Header="Khách hàng" Binding="{Binding Customer.CustomerFullName}" Width="*"/>
                            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalPrice, StringFormat=\{0:N0\}}" Width="120"/>
                           
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="1" Margin="4,10,0,4" VerticalAlignment="Center" Foreground="{StaticResource HeaderTextBrush}">
                        <Run Text="Tổng: "/>
                        <Run Text="{Binding Reservations.Count, Mode=OneWay}" FontWeight="Bold"/>
                        <Run Text=" đặt phòng"/>
                    </TextBlock>
                </Grid>
            </Border>

            <Border Grid.Column="1" Padding="16" Style="{StaticResource CardBorder}" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Chi tiết đặt phòng" 
                               FontWeight="Bold" FontSize="18" 
                               Margin="0,0,0,16" 
                               Foreground="{StaticResource PrimaryBrush}"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Mã:" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelTextBlock}"/>
                        <TextBlock Text="{Binding SelectedReservation.BookingReservationId}" Grid.Row="0" Grid.Column="1" FontWeight="SemiBold" VerticalAlignment="Center"/>

                        <TextBlock Text="Ngày đặt:" Grid.Row="0" Grid.Column="2" Style="{StaticResource LabelTextBlock}" Margin="20,0,8,0"/>
                        <TextBlock Text="{Binding SelectedReservation.BookingDate, StringFormat=\{0:yyyy-MM-dd\}}" Grid.Row="0" Grid.Column="3" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        
                        <TextBlock Text="Khách hàng:" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelTextBlock}" Margin="0,8,8,0"/>
                        <TextBlock Text="{Binding SelectedReservation.Customer.CustomerFullName}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,8,0,0"/>

                        <TextBlock Text="Tổng tiền:" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelTextBlock}" Margin="0,8,8,0"/>
                        <TextBlock Text="{Binding SelectedReservation.TotalPrice, StringFormat=\{0:N0\}}" Grid.Row="2" Grid.Column="1" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,8,0,0" Foreground="{StaticResource PrimaryBrush}" FontSize="14"/>
                    </Grid>

                    <DataGrid ItemsSource="{Binding SelectedReservation.BookingDetails}"
                              Style="{StaticResource CustomDataGrid}"
                              MaxHeight="250" Margin="0,8,0,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã phòng" Binding="{Binding RoomId}" Width="80"/>
                            <DataGridTextColumn Header="Số phòng" Binding="{Binding Room.RoomNumber}" Width="100"/>
                            <DataGridTextColumn Header="Từ" Binding="{Binding StartDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"/>
                            <DataGridTextColumn Header="Đến" Binding="{Binding EndDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"/>
                            <DataGridTextColumn Header="Giá/ngày" Binding="{Binding ActualPrice, StringFormat=\{0:N0\}}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
                        <Button Content="Tạo đặt phòng mới" Command="{Binding ShowCreateDialogCommand}" Style="{StaticResource PrimaryButton}"/>
                        <Button Content="Hủy đặt phòng"
                                Command="{Binding CancelReservationCommand}"
                                CommandParameter="{Binding SelectedReservation.BookingReservationId}"
                                Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                        <Button Content="Xem phòng trống" Command="{Binding OpenAvailabilityCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    </StackPanel>

                </StackPanel>
            </Border>
            
        </Grid>
        
    </Grid>

    
</UserControl>